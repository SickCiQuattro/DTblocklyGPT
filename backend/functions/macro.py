# This file has been intentionally removed.
# All task lifecycle logic (publish, save_draft, discard_draft) is now
# handled by backend/functions/task_lifecycle.py, which correctly
# distinguishes between 'task' and 'macro_task' types.
# The legacy MACRO API routes (/api/macro/*) have been removed from endpoints.py.
