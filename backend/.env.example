FLASK_BRIDGE_URL = "http://localhost:5000"

LLM_PROVIDER = "gemini" # gemini | openai | ollama

LLM_MODEL = "gemini-3.5-flash-lite" # 500 model request per day

OPENAI_API_KEY = ""
GEMINI_API_KEY=""

# Optional — overrides the key/endpoint for whichever LLM_PROVIDER is active above
# LLM_API_KEY = ""
# LLM_BASE_URL = ""

# Optional — only needed with LLM_PROVIDER = "ollama", and only when Ollama runs
# somewhere other than this machine. Defaults to http://localhost:11434/v1.
# Keep the /v1 suffix: chat.py talks to Ollama's OpenAI-compatible endpoint.
# OLLAMA_BASE_URL = "http://localhost:11434/v1"