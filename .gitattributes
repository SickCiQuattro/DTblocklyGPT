# Shell scripts must always check out with LF endings — CRLF breaks bash on
# Linux/WSL ("$'\r': command not found" / "bad interpreter"). This overrides a
# contributor's core.autocrlf=true (Git for Windows default).
*.sh   text eol=lf
*.bash text eol=lf
